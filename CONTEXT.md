# 当前上下文

本文件是项目恢复入口。每次实质任务结束前必须更新，并保持简短。

## 状态

- 最近更新：`2026-06-16 Asia/Shanghai`
- 当前阶段：`进行中`
- 当前主要角色：`项目主脑 + 知识库`
- 最近完成：将项目文件归档到桌面 Workspace 的独立项目目录，并保留原位置副本避免当前页面或临时分享中断。
- 当前目标：以后围绕本项目的修改默认在 `site/` 内进行。

## 下一步

1. 如需继续修改页面，先进入 `site/`。
2. 如需重新分享手机链接，从 `site/` 运行 `share-site.ps1` 或 `一键分享网站.bat`。

## 阻塞

- 无。

## 当前 TODO

- [ ] 下次需要发手机时，基于 `site/` 重新生成临时分享链接。

## 最近验证

- 命令或检查：确认 `site/` 已复制当前网页项目文件，核心项目文档已初始化。
- 结果：项目目录可作为后续工作入口。

## 恢复所需文件

- `README.md`
- `REQUIREMENTS.md`
- `TECH_STACK.md`
- `DECISIONS.md`
- `site/index.html`
- `site/styles.css`
- `site/script.js`

## 最近交接

- 来源角色：Codex
- 接收角色：下一次 Codex 会话
- 交接内容：当前可运行网页位于 `site/`；原始位置 `D:\Documents\New project` 暂保留为兼容副本。

## 更新规则

- 只保留当前状态，不堆积历史。
- 完成事项移入 `DEVELOPMENT_LOG.md`。
- 稳定决策移入 `DECISIONS.md`。
- 未解决缺陷移入 `BUG_TRACKER.md`。
